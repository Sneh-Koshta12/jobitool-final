This is a great idea. As an AI, while I have access to our current conversation context, having a Project Manifest ensures that if we ever start a fresh session, we can pick up exactly where we left off without losing a single line of logic.Here is the full technical description of your Two-Sided AI Job Marketplace as it stands today, March 8, 2026.🚀 Project Manifest: AI Job & Candidate Matcher1. Core ArchitectureStack: Python (Backend), Supabase (Vector Database), React (Frontend - Ready to start), Hugging Face (Local AI Model).AI Engine: sentence-transformers/all-MiniLM-L6-v2 (Local execution, 384-dimension vectors).Database: PostgreSQL with the pgvector extension enabled in Supabase.2. Database Layer (Supabase)We have established two primary tables and two custom SQL functions:ComponentPurposeStatusjobs TableStores job postings with AI embeddings.Fully Populated (India Job Market Data).candidates TableStores candidate profiles and extracted PDF text.Ready for Uploads.match_jobsSQL function calculating Cosine Similarity for Job Seekers.Deployed.match_candidatesSQL function calculating Cosine Similarity for Recruiters.Deployed.3. Backend API Layer (FastAPI)The main.py server is complete and tested via Uvicorn/Swagger UI.PDF Processing: Uses pdfplumber for robust text extraction from resumes.Endpoints:POST /upload-resume/: Accepts candidate data + PDF, vectorizes locally, and saves to candidates table.GET /search-jobs/: Allows candidates to perform semantic search against the jobs table.GET /search-candidates/: Allows HR/Companies to perform semantic search against the candidates table.4. Frontend Layer (React)Status: Initialized (npx create-react-app frontend).Dependencies: axios installed for API communication.📝 The "Remind Me" PromptIf you ever need to restart this conversation, just copy and paste the block below:"I am Sneh Koshta, working on a two-sided AI Job Recommendation system.Current Progress:Backend: FastAPI is finished with pdfplumber and sentence-transformers. It connects to Supabase via SUPABASE_URL and SUPABASE_KEY.Database: Supabase has a jobs table (pre-filled) and a candidates table (ready for uploads). Both use 384-dim vectors. SQL functions match_jobs and match_candidates are deployed.Frontend: React app is initialized and axios is installed.Next Step: We are ready to build the React UI components for the Candidate Dashboard (Resume Upload + Job Search) and the Recruiter Dashboard."